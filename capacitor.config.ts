import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wangbo.runner',
  appName: '王博晋升之路跑酷',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;


